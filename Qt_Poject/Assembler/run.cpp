#include <iostream>
#include <string>
#include <cstdio>
using namespace std;
struct lable{
    string lab;
    int position;
};
struct library{
    struct lable all[100];
    int place;
}lib;

int main()
{
    string input;
    int result = 0;
    int lineNum = 1000;
    freopen("1.txt","r",stdin);
    scanf("%s",input);
    while(input!="END"){
        switch (input=="add") {
        case value:

            break;
        default:
            break;
        }
    }
}
